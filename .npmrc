# Refuser l'installation sous une version de Node antérieure à celle déclarée
# dans « engines ». Sans cette ligne, npm se contente d'un avertissement : la
# publication a déjà échoué sur un Node 20 avec un message trompeur
# (« Missing: esbuild@… from lock file ») au lieu de nommer la vraie cause.
engine-strict=true
